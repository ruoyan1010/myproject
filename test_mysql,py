import pymysql

conn = pymysql.connect(
    host='localhost',
    user='root',
    password='123456',
    database='test'
)

cur = conn.cursor()
cur.execute("SELECT 1;")
print(cur.fetchall())

cur.close()
conn.close()
